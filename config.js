// config.js
const config = {
    HIGH_PAY_THRESHOLD: 10,  // Set your own value
    MAX_DISTANCE: 5,          // Distance in miles
    CHECK_INTERVAL: 5000      // Time in milliseconds to check for new orders
};

module.exports = config;